#include "main.h"
/**
 * mul - A function that multiplies two integers
 * @p: first integer to multiply
 * @q: second integere to multiply
 * Return: multiplication of @p and @q
 */
int mul(int p, int q)
{
	return (p * q);
}
